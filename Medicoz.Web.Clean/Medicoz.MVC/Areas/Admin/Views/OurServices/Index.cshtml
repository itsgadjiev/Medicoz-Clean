@using Medicoz.MVC.Areas.Admin.ViewModels;
@model OurServicesListVM
@{
    int counter = 1;
}
<div class="row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-striped mb-0">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Icon</th>
                                <th>Title</th>
                                <th>Description </th>
                                <th class="text-end">Action
                                    <a class="btn btn-success" asp-action="create" asp-controller="ourservices">Create</a>
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.OurServices)
                            {
                                <tr>
                                    <td>@(counter++)</td>
                                    <td>@Html.Raw(item.Icon)</td>
                                    <td>@Model.Title.GetValueOrDefault(item.Id, "Default TestModel Title")</td>
                                    <td>@Model.Desc.GetValueOrDefault(item.Id, "Default TestModel Description")</td>
                                    <td class="text-end">
                                        <a asp-action="update" asp-controller="OurServices" asp-route-id="@item.Id"><i class="las la-pen text-secondary font-16"></i></a>
                                        <a href="#"><i class="las la-trash-alt text-secondary font-16"></i></a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table><!--end /table-->
                </div><!--end /tableresponsive-->
            </div><!--end card-body-->
        </div><!--end card-->
    </div> <!-- end col -->
</div> <!-- end row -->

